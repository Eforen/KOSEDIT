{
  "scripts": {
    "build": "gulp build",
    "deploy": "gulp deploy"
  },
  "devDependencies": {
    "gift": "^0.10.2",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1"
  },
  "dependencies": {
    "gh-pages": "^2.0.1"
  }
}
